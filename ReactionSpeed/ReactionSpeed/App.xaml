<Application x:Class="ReactionSpeed.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ReactionSpeed"
             xmlns:viewModels="clr-namespace:ReactionSpeed_Client.ViewModels"
             xmlns:views="clr-namespace:ReactionSpeed_Client.Views"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type viewModels:TestViewModel}">
            <views:TestView/>
        </DataTemplate>
         
    </Application.Resources>
</Application>
